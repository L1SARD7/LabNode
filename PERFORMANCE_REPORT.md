# Звіт з продуктивності (Lab 7)

**Дата:** 31.12.2025
**URL веб-додатку:** https://gamepedia-app.onrender.com/games
**Інструмент:** Google PageSpeed Insights (Lighthouse)
**URL результатів аналізу front-end:** https://pagespeed.web.dev/analysis/https-gamepedia-app-onrender-com-games/282ep9fa22?form_factor=desktop&category=performance&category=accessibility&category=best-practices&category=seo&hl=uk&utm_source=lh-chrome-ext

---

## 1. Front-end Audit (Результати аналізу)

Проведено аудит клієнтської частини веб-додатку. Завдяки використанню серверного рендерингу (EJS) та відсутності важких JS-фреймворків, додаток демонструє відмінні показники продуктивності.

### Загальні оцінки (Desktop)
| Категорія | Оцінка | Статус |
|-----------|--------|--------|
| **Performance** | **98/100** | 🟢 Відмінно |
| **Accessibility** | **100/100**| 🟢 Відмінно |
| **Best Practices**| **100/100**| 🟢 Відмінно |
| **SEO** | **100/100**| 🟢 Відмінно |

### Ключові метрики (Core Web Vitals)
* **First Contentful Paint (FCP):** 0.4s — контент з'являється миттєво.
* **Largest Contentful Paint (LCP):** 0.6s — основний блок завантажується швидше за 2.5с (норма Google).
* **Cumulative Layout Shift (CLS):** 0 — верстка стабільна, елементи не "стрибають" при завантаженні.
* **Speed Index:** 0.5s — сторінка візуально готова майже одразу.

### 🛠 Виконані технічні оптимізації
На основі рекомендацій Lighthouse було впроваджено:

1.  **Text Compression (Gzip):**
    * Підключено middleware \`compression\` у \`app.ts\`.
    * Зменшено розмір передаваних HTML/CSS/JS файлів на 60-70%.

2.  **Efficient Cache Policy:**
    * Статичні файли (CSS, зображення) роздаються через \`express.static\`.
    * Браузер кешує ресурси, зменшуючи навантаження на мережу при повторних візитах.

3.  **Render-blocking Resources:**
    * CSS підключено в \`<head>\` для швидкого першого відмальовування.
    * Скрипти перенесено вниз сторінки або виконуються асинхронно.

4.  **Accessibility (Доступність):**
    * Всі поля вводу мають прив'язані \`<label>\`.
    * Кнопки та посилання мають достатній контраст та розмір для натискання.
    * HTML структура семантична (\`header\`, \`main\`, \`section\`).

---

## 2. Load Testing (Результати)
**Сценарій:** 30 секунд, 20 конкурентних користувачів.
**Результати:**
- **Всього запитів:** 6217
- **RPS (Запитів на секунду):** ~207 req/s
- **Середній час відповіді:** 48.35 ms
- **Помилки:** 0%

---

## 3. Resource Usage (CPU & RAM)
**CPU Profiling (Аналіз `cpu-profile.txt`):**
- **JavaScript Execution:** ~29.5%
- **Garbage Collection:** ~2.2% (Ефективне управління пам'яттю).
- **Hotspots (Найважчі функції):**
  1. `scanLine` / `compile` (модуль `ejs`) — рендеринг HTML шаблонів.
  2. `writevGeneric` — мережевий I/O (відправка відповідей).
  3. `RegExp` — парсинг тегів шаблонізатора.

  ### **Memory Usage (RAM)**
Моніторинг процесу `node` під час тесту:
- **Idle (Спокій):** ~63 MB
- **Peak Load (Пік):** ~113 MB
- **Stabilized (Стабілізація):** ~113-116 MB

**Висновок:** Пам'ять зросла під навантаженням і стабілізувалася. Неконтрольованого росту (Memory Leaks) не виявлено.

---

## 4. Database Analysis
**Query Performance:**
Аналіз запиту `SELECT * FROM games` (через `EXPLAIN ANALYZE`):
- **Planning Time:** 1.599 ms
- **Execution Time:** 0.100 ms
- **Стратегія:** `Seq Scan` (Послідовне сканування).